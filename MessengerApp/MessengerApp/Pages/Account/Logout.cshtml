@page
@model MessengerApp.Pages.Account.LogoutModel
@{
    ViewData["Title"] = "Log out";
}

<div class="row justify-content-center">
    <div class="col-md-6 text-center">
        <h2>@ViewData["Title"]</h2>
        <p class="mt-3">Are you sure you want to log out?</p>

        <form method="post">
            <button type="submit" class="btn btn-primary mt-3">Yes, log me out</button>
            
            @* <button onclick="history.back()" class="btn btn-secondary mt-3">
                <i class="bi bi-arrow-left"></i> Назад
            </button> *@
            @* <a asp-page="/" class="btn btn-secondary mt-3">Cancel</a> *@
        </form>
    </div>
</div>